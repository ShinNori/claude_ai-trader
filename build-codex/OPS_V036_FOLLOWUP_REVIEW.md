# ops v0.3.6 第10回・独立レビュー追補

確認開始: 2026-09-09 06:29 JST（時計確認）。対象はClaude実装のv0.3.6。自動連携とは別枠。

## 結果

**全体402件中401通過・1失敗、11.29秒。skip/xfailなし。全件通過ではない。** 失敗は本レビューで追加したO01。元の388件および並行更新で存在したN01〜N05の7ケースは通過している。

| 実測段階 | 結果 |
|---|---|
| 受領した最新作業ツリー（元388＋既存N系列7件） | 395 passed in 10.83s |
| 本レビューのO系列7件のみ | 1 failed, 6 passed in 0.54s |
| 指定の全体試験（395＋O系列7） | 1 failed, 401 passed in 11.29s |

実行ディレクトリはops/。コマンド本体は `python -m pytest ../common/tests/phase2 ../build-codex/tests tests -q`。実際には同梱Pythonの絶対パスを使用し、`-p no:cacheprovider --basetemp <OS一時フォルダ>` を追加。PYTHONDONTWRITEBYTECODE=1、PYTHONUTF8=1、試験用依存ライブラリをOS一時フォルダのPYTHONPATHで読んだ。台帳はすべてpytestの合成一時台帳。既存環境に不足していたpytest/duckdb/PyYAML/requestsは一時フォルダへインストールした。実サービス・実LLMは使用していない。

## 指定変更の確認

新しい依頼を受けてファイルを再読した時点で、以下は既に共有作業ツリーへ反映されていた。本セッションの編集と偽って数えず、既存変更として維持・検証した。

- test_ops_v035_review.pyのL03: 10:00の保留に対する08:00 DISCARDをLedgerErrorにし、seq/残高/保留不変を検査。同時刻解決と09:00の残高再生、解決時点のreplay_known一致を検査。
- test_v03_integrity.py: 解決日時がAT+1hとなり、actor監査と再起動後pendingなしのassertは維持。
- test_migration.py: legacyフィクスチャおよびtest_m03の2接続がclosing()とトランザクションの二重contextになっている。元のassertは維持。N01のgc前後原本hash検査も通過。

Bの「09:00のreplayは保留行を含む」という説明は、10:00業務時刻の保留を09:00へ含める趣旨なら既存の業務時刻契約と衝突する。また公開replay戻り値LedgerViewにはpending欄がない。今回の検証は、解決前の現在pendingをpending_rowsで確認し、09:00のreplayは残高不変を確認したもの。履歴存在の確認と過去残高ビューの確認を区別し、未来保留の包含を実測したとは報告しない。

## 新規指摘

| ID | 重大度 | 事象・根拠 | 修正案 |
|---|---|---|---|
| O01 | A | 09:00業務時刻のCSVを保留→11:00作成時刻の通知targetを登録→10:00にtargetへAPPLYすると受理され、現金900000・100株になる。しかしreplay(10:00)でLedgerError「通知 target がありません」。ledger.py:849前後のneededがTRADE/CSV_FILLのみでPENDING_RESOLVEDの確定参照を含まず、将来通知を補完しない。686行で停止する。 | 既存の遅着約定と同じ方針で、受理済みAPPLYの確定通知参照を時点再生時に補完する。未来の予約・通知状態を持ち込まず、解決前の現金を先取りしない。通知時刻による入力拒否へ変えるなら契約変更として別途明示し、試験の期待値を黙って弱めない。 |

再現ファイル: `tests/test_ops_v036_followup_review.py::test_o01_resolved_pending_before_late_notice_replays`。初期現金1000000、価格1000×100株。解決時刻は保留業務時刻以後でありL03の新制約を満たす。現時点残高は壊れていないが、公開APIで受理した履歴の時点再生ができないためAとした。一般的な「不明参照を無視する」修正は不可。

## 追加7件の範囲

| 試験 | ケース数 | 結果・対象 |
|---|---|---|
| O01 | 1 | 失敗。遅着通知を参照する保留APPLYの時点再生 |
| O02 | 2 | 通過。通常/provenance付き台帳で、実約定と実訂正を適用した後のSNAPSHOT前空判定、訂正前後残高、UTC表記、再起動 |
| O03 | 1 | 通過。時刻なし保留APPLYは解決日時で残高へ反映し、それ以前を先取りしない |
| O04 | 2 | 通過。移行マーカーなし/マーカー後の逆順解決に対してMigrationErrorのseq/kind、実migrate --checkのJSON位置、原本SHA256不変 |
| O05 | 1 | 通過。正規移行後の保留・同時刻DISCARD、旧最終seqとの残高一致、開始残高前空判定 |

既存N系列・Claude M系列も全体試験で実行し、EXTERNALの売却予約・cutover保留TRADE・別オフセット同時刻・拒否時の残高/保留/seq維持・gc後原本不変を確認した。N系列の作成者・元の実測結果を本セッションが代行したとは扱わない。

## 契約反映と文書上の区別

親フォルダの改訂案v1.1へ(e)開始残高前は空、(f)解決時刻制約、(g)strip範囲を追記した。共通仕様本文は変更しない。

- 早い解決の拒否はledger_eventsを増やさないが、ingest_attemptsにはREJECTED/VALIDATION監査が残る。文書の「履歴に残らない」は残高イベントに限定して説明する。
- migrate --checkは、マーカーなし逆順履歴ではexit0のviolation欄、移行マーカー後の逆順履歴ではexit1のMigrationError欄にseq/kindを返した。どちらも位置を取得できるが、呼出元は両形式を処理する必要がある。JSON形式統一は新たな仕様変更としてここでは実装しない。
- 旧規則区間をマーカーで保持する契約は維持する。旧規則2で許容される履歴まで新規則3の拒否に無断変更しない。

次のClaude作業はO01修正と必要な文書訂正・再検証。[開発ループのClaude依頼](Claude引き渡しプロンプト.md)へ保存する。自動連携のH系列は別枠へ保持する。ops本体・既存受入テストの弱体化・実台帳操作は行っていない。

終了時刻: 2026-09-09 06:40 JST
次に渡す一文（コピー用）:
```text
ai-trader の作業フォルダで、build-codex/Claude引き渡しプロンプト.md を読み、「A. 固定プロンプト」と「B. 今回の依頼」に従い、ops v0.3.6のO01（遅着通知への保留APPLY後の時点再生エラー）修正と文書明確化・独立再検証を行ってください。
```