# ops v0.3.9 独立レビュー（第13回）

確認開始: 2026-09-09 08:52 JST（実機時計）。対象はClaude実装v0.3.9。AGENTS/Aの役割分担を維持し、opsは読取のみ。

## 結果

**最終487件全件通過（14.25秒）、失敗・skip・xfailなし。** T06の3経路とT07の採用契約を確認した。今回の検証範囲では新規の実装不具合を検出していない。文書の互換範囲・ID省略時の説明は下記C01/C02の明確化をClaudeへ依頼する。

| 段階 | 実測 |
|---|---|
| T07期待値更新後の既存全体 | 469 passed in 14.80s |
| 追加U系列の初回 | 17 passed / 1 failed in 0.92s。失敗はCodex作成中試験の誤前提（時刻なしCSVは明示IDがあっても保留）。既存契約に合わせ、保留を確認してAPPLYする試験へ訂正 |
| 追加18件を含む最終全体 | 487 passed in 14.25s |

実行場所ops/、コマンド本体は `python -m pytest ../common/tests/phase2 ../build-codex/tests tests -q`。同梱Python・既存の一時ライブラリを使用し、`--tb=short -p no:cacheprovider --basetemp <OS一時フォルダ>`、PYTHONDONTWRITEBYTECODE=1、PYTHONUTF8=1を追加。DBはOS一時フォルダの合成台帳だけ。実口座・実LLM・LINE・常駐処理なし。

## T07期待値更新

build-codex/tests/test_ops_v038_review.pyの既存T07だけを、採用済み契約へ更新した。時刻なし訂正(900円)の受理後、09:00現金1000000、10:00現金964000。09:00の再訂正はapplied=False、errorに「訂正の業務時刻」、残高・seq不変。同時刻10:00の950円再訂正は受理され962000。既存の残高・seq検証を維持し、必要なassertを追加。skip/xfail・条件緩和なし。Q09更新と時計依存フィクスチャのat=ATも維持。

## 独立試験U系列（18件）

| ID | 件数 | 検証 |
|---|---|---|
| U01 | 6 | TRADE、明示ID付きCSV、未知ID保留CSV × 時刻あり/なし。時刻なし訂正→時刻なし訂正→日時付き訂正。直接約定は約定時刻、APPLYは解決時刻を継承。同時刻の別UTCオフセット、直前拒否、再起動・replay_known・表示at=NoneとDB不変。時刻なしCSVは既存規則により保留となり、解決時刻を継承 |
| U02 | 2 | 同じ通知の独立した2約定へ日時付き訂正と時刻なし訂正を逆順で記録。さらに日時付き訂正を時刻なし再訂正。09:00=960000、10:00=941000、11:00=943400、60株で一致。記録順を勝手に時刻順へ並べ替えない |
| U03 | 6 | NOTICE_STATEの不明通知、時刻なしCORRECTIONの不明対象を合成故障注入。各replayはLedgerError、reopen/checkはseqとkind付きMigrationError。不明参照を読み飛ばさない |
| U04 | 2 | 時刻なし訂正の継承時刻より早い再訂正を旧履歴として合成。マーカーなしcheckはexit0のviolation、マーカー後はexit1のMigrationError。実CLIでseq/kind/reason・原本SHA256不変を確認。正規migrate.applyで旧規則区間へ保存すれば受理済み残高962000を保持し、マーカー後の新操作にはQ09を適用 |
| U05 | 2 | cutover前/時刻なしTRADE保留、時刻なし訂正もcutover保留。両方APPLYを拒否し、現在残高・再起動・解決時点非先取りを確認。正常な「cutover保留TRADEがAPPLYされた後」という経路は存在しない |

すべて最終全体で通過。試験本体はbuild-codex/tests/test_ops_v039_review.py。SQLは不正な旧履歴・参照欠損の合成故障注入のみ。実運用台帳を書き換える手順ではない。

## 文書照合と残る説明事項

T06のエラー、T07の明示ID経由の継承、表示時刻None、対象自体が時刻なしの場合の継承なし、旧履歴の検出位置はREADME/Claude対応結果と実測が一致する。R06/R07のコメント訂正・予約60120のassert追加も反映済み。

- C01（文書）: 「旧v0.2区間は対象外」はQ09の入力制約に限定して明記すべき。_State._on_tradeの比較はrule_version>=3だが、有効時刻を継承する行(ledger.py:593)には版判定がなく、_effective_rows(900–925)も全履歴に適用する。旧区間で入力拒否を追加しないことと、旧区間の時刻フィルタも変わらないことは別。U04で旧区間の受理残高維持・マーカー後拒否を確認した。過去版そのものとの全時点表示互換を検証したとは主張しない。
- C02（文書・既知の対応範囲）: Claude対応結果の「replaces_event_idなしは継承できず従来どおり」は正確には時点再生の事前走査の制限。状態側は直前の有効約定を選択してeffective_atを継承するが、_effective_rowsは明示IDなしでは継承時刻を求めない。この二つを区別して記載すること。明示IDなしの時刻なし訂正を本レビューの継承保証対象に含めない。公開モデルはreplaces_event_id=Noneを許し、共通仕様にも直前対象が記載されるため「呼び出し側が常に指定」をAPI保証と読み替えない。実コード検索では、本番の訂正入力アダプタによる必須化は確認できなかった。今回は既にBに記載された制限の明確化であり、新しい入力拒否契約を追加していない。

以上2点は文書の明確化依頼であり、今回の487件通過を取り消す新規失敗ではない。将来ID省略経路まで整合保証を広げる場合は独立した契約・試験を設ける。共通仕様本文を推測で変更しない。

親の改訂案v1.1へ(k)を追記し、明示ID付き時刻なし訂正の有効時刻継承・非先取り・表示時刻・互換範囲を明記。第12回T06未達は今回解消、T07は採用契約で解消したことを追記した。

## 変更範囲と次工程

変更: build-codex/tests/test_ops_v038_review.pyのT07、追加U試験、今回報告、README、Claude向けB、common/ISSUES.md追記、親の改訂案(k)。ops・共通仕様本文・自分宛てBは未変更。

ops/aitrader_ops/ledger.py SHA256（開始・終了前確認一致）: E222EDED9C6D02D5861C71A2AAE696170B3419A632D855BD75767DB6C9BDB94F。

次のClaude依頼はC01/C02の文書明確化と今回の試験・改訂案の最終照合。追加の実装課題がなければ引き渡し不要として開発ループを閉じる。自動連携自身のレビュー・常駐起動確認は別枠で、台帳試験の通過から稼働可とは判定しない。
終了時刻: 2026-09-09 08:56 JST
次に渡す一文（コピー用）:
```text
ai-trader の作業フォルダで、build-codex/Claude引き渡しプロンプト.md を読み、「A. 固定プロンプト」と「B. 今回の依頼」に従い、ops v0.3.9の独立レビュー結果・改訂案(k)の最終照合と、旧規則および訂正対象ID省略時の文書明確化を行ってください。
```
