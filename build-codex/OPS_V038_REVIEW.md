# ops v0.3.8 独立レビュー（第12回）

確認開始: 2026-09-09 08:14 JST（最初の実機時計取得）。依頼発行時刻08:30は未来だったため転記しない。

## 結果

Q09期待値更新後の既存442件は全通過（14.24秒）。追加T系列15件を含む最終は **457件中453通過・4失敗（11.57秒）、skip/xfailなし**。全件通過ではない。ops本体は読取のみ。

実行場所ops/。指定の `python -m pytest ../common/tests/phase2 ../build-codex/tests tests -q` に、最終は `--tb=line -p no:cacheprovider --basetemp <OS一時フォルダ>` を追加。同梱Pythonと既存の一時試験ライブラリを使用、PYTHONDONTWRITEBYTECODE=1。実台帳・実口座・実LLM・LINEは操作していない。

## 指摘

| ID | 分類 | 再現・影響・依頼 |
|---|---|---|
| T06 | A: エラー契約未達、3ケース | 合成履歴のNOTICE_CREATED/NOTICE_STATEを除去しEXPIREだけがtargetを参照する故障注入。replay、再起動、migrate.checkのすべてでledger.py:375の直接辞書参照からKeyErrorが漏れる。黙って無視はしないが、契約のLedgerError / 位置付きMigrationErrorにならない。EXPIREの不明IDを明示検証し、既存の位置付きエラーへ接続すること。checkでは旧規則再構築も不能なのでviolationの正常戻り値ではなくMigrationErrorを期待する。正常API履歴での残高破壊を示すものではなく、旧版から存在した経路で今回新規退行とは断定しない。 |
| T07 | B: 時刻なし訂正の契約の隙間、1ケース | 09:00保留40株→14:00作成通知へ10:00 APPLY→時刻なし訂正(900円)→それを09:00で再訂正(950円)は両方受理。しかしreplay(09:00)では10:00 APPLYが除外され、時刻なし訂正が対象c1不在で停止する。後段の再訂正以前に、時刻なし訂正単体も時点フィルタを通る。effective_at=Noneによって次の時刻比較もなくなる。採用済み「時刻なし訂正は制約なし」と非先取りの整合を決める必要がある。未来APPLYの一律適用・不明参照の無視で解消しないこと。入力拒否に変更する場合は明示的にCodexへ期待値変更を依頼する。 |

反証はbuild-codex/tests/test_ops_v038_review.py。失敗はskip/xfailにせず維持。T07は新しい表示残高を勝手に決めず、受理した履歴が再生で落ちることを示す。

## 更新と網羅範囲

Q09は既存関数名を保持し、解決前訂正のapplied=False・error文言・残高/seq/replay不変、09:30現金1000000、10:00現金960000、同時刻の訂正受理後964000を検証する期待値へ更新。assertの単純削除・条件緩和ではなく、Claude採用の入力拒否契約への更新。時計依存フィクスチャのat=ATは維持。

| 試験 | 件数 | 結果 |
|---|---|---|
| T01 部分APPLY合算→訂正→訂正の訂正、時刻なし保留×取消前後、別オフセット、再起動 | 4 | 通過。40+20株、最終942000・60株、予約は取消時0/それ以外40080 |
| T02 SELL価格のみ二重訂正 | 1 | 通過。取得単価600を維持、60株・現金1038000 |
| T03 移行前の前倒し通常約定訂正、マーカー後拒否、check位置 | 1 | 通過。旧区間の現在残高964000を保持、途中時点再生は従来の対象欠落。マーカー後の不正履歴を位置付きTRADEエラーで拒否 |
| T04 REJECTEDのcontext_only識別補完→APPLY拒否 | 1 | 通過。SKIPPED遮断、保留/seq不変、予約0 |
| T05 EXPIRE列挙外の識別非補完 | 1 | 通過。時点外の状態参照と独立させ、_on_notice_createdの観測でtargetだけが補完されotherは補完されないことを確認 |
| T06 不明EXPIRE参照のreopen/replay/check | 3 | 失敗（上記） |
| T07 時刻なし訂正の連鎖 | 1 | 失敗（上記） |
| T08 通常の時刻なし約定を対象にした日時付き訂正 | 1 | 通過。現金964000、再起動一致 |
| T09 cutover遮断TRADE、時刻なし/境界前 | 2 | 通過。APPLY自体が拒否されるため「APPLY後の訂正」という正常経路は存在しない。訂正による迂回も不可 |

## 文書照合

README第12回「通常約定の前倒し訂正も従来から再生不能」は、T03の合成旧履歴を正規migrate.applyで旧規則2区間へ保存した実験で、現在残高とreplay_knownは成立する一方replay(09:00)は訂正対象欠落になることを確認。過去版バイナリそのものを再実行したという意味ではない。

Claude R07は「列挙外」とコメントされたotherも実際はEXPIRE集合に含むため、列挙外の識別非補完の証拠としては不足。T05で別途直接観測した。R06コメントの「EXPIREDなので残60株予約なし」は実装と異なる（assert自体は現在残高との一致のみ）。_on_expireは予約を解放しないため、Q04同様、作成時刻以後の部分約定予約は60120。最新READMEはこの誤記を含まないが、試験コメントをClaudeに訂正依頼する。

改訂案v1.1へ(i)訂正時刻制約と(j)状態/期限切れ識別補完を追記。共通仕様本文は未変更。採用契約とT06/T07の未達・残課題を分けた。

## 版・次工程

ops/aitrader_ops/ledger.py SHA256（確認前後一致）: 2C654B628BE9BF288240C54482A750BA2787407BF465863995A0994D7C83706F。

次はClaudeのT06修正・T07契約判断と対応・独立再検証。Claude宛てBを完成後にdevチャネルへ公開する。ファイル公開はClaudeの起動・送信完了を意味しない。自動連携自体のレビュー・起動確認とは別工程。
終了時刻: 2026-09-09 08:21 JST
次に渡す一文（コピー用）:
```text
ai-trader の作業フォルダで、build-codex/Claude引き渡しプロンプト.md を読み、「A. 固定プロンプト」と「B. 今回の依頼」に従い、ops v0.3.8レビューのT06（EXPIRE不明参照）の修正とT07（時刻なし訂正）の契約決定・対応および独立再検証を行ってください。
```
