# AGENTS.md — Codex への引き継ぎ（ai-trader）

## 最新上限：50%へ変更（2026-09-17 14:32 JST）

ユーザー「Astra50%まで」を受け停止上限を50%へ変更し開発を再開する。取得できる公式使用率はAstra単独でなくCodexアカウント共通値のため、この値で50%を管理する。開始時使用41%・残59%。旧40%停止記録より優先。子も同じ停止方針に従い、実作業は適切なモデルへ委譲する。追加リセット・自動公開送信・見張り再開は許可していない。


## 最新指示：Astraの実作業を適切なモデルへ委譲（2026-09-17）

ユーザーはAstraの使用量削減のためサブエージェント活用を指示。Codex側の旧「原則0」を見直し、独立した実装・試験・文書作業があり親も統合等を並行できる場合、通常1体へ委譲する。機械的作業はgpt-5.6-luna/low、小さな確定修正はgpt-5.6-terra/medium、通常実装・不具合修正・試験はgpt-5.6-sol/medium。複雑な未解決事項だけAstraへ戻す。独立作業がなければ無理に起動しない。

Astra親は短い振分け・依存整理・重要差分の最終判断に限定し、子の実装/試験/長文要約をやり直さない。起動時にmodelとreasoning_effortを明示し、fork_turns=noneで対象ファイル・契約・完了条件だけ渡す。最大3体は維持するが常時最大起動はしない。モデル切替2回上限、Claude側Sonnet層限定/Opus並列禁止は維持。

この方針は親モデル自体の変更ではない。アカウント使用40%上限も変更せず、上限超過中に別モデルの子を起動して開発を継続しない。再開後の既定として適用する。次のv2 CLIは仕様確認を親、実装・局所試験をSolが担当する候補。実接続・自動送信・見張り再開の許可ではない。


## 最新方針：担当分離・トークン節約（2026-09-16）

ユーザーの新指示により、[担当分担とモデル選択](build-codex/TEAM_WORKFLOW.md)を優先する。Codexは製品実装・修正・統合試験、Claudeは未決契約と独立境界試験・重要差分確認を担当。同一作業の二重実施・全件レビュー往復を止める。通常モデルを基本に難所だけ上位へ切替。サブエージェントは原則0、必要な独立作業だけ起動し、以前の「毎回最大3体」「Astra/Opus固定」より優先する。自動送信・起動・見張り再開は許可されていない。詳細は上記文書を参照し、以下の旧担当/モデル/並列方針との衝突は本節を優先。


## 最新上限（2026-09-12）

ユーザーが残60%まで継続を指示。新上限は使用40%・残60%。旧20%停止記録より優先し、R2対応・再検証・第3回依頼作成を再開する。

## 20%まで確認なしで継続（2026-09-12）

Claude Opusの証拠履歴レビューへの応答はbuild-codex/HISTORY_VALIDITY_BINDING_PLAN.mdのF-01〜F-08固定設計を参照。コード/IDとreceipt時刻正規化は次の結合版の設計であり、既存v1受入を無言変更しない。F-09は防御処理、F-10はCLI上限の説明/試験として対応。

保存案の人工受入としてevidence_history_fixture_v1を追加。取得重複・線形訂正・記録時点による過去選択を純粋なメモリ内モデルで検査する。契約はbuild-codex/EVIDENCE_HISTORY_FIXTURE.md。DB保存や実証拠の真正性を採用した意味ではない。

現在の次工程として、人工証拠が明示する単元・イベントの適用期間を選択原本の値へ結ぶstrict_validity_fixture_v1を実装する。valid_from<=as_of<valid_untilの半開区間を人工入力契約とし、実提供元の期間規則や公表/取得時刻は推測しない。契約はbuild-codex/STRICT_VALIDITY.md。DB取込や訂正履歴保存を採用した意味ではない。

ユーザーは「20%まで確認せずに進めて」と指示した。使用率を段階ごとに確認し、20%以上で新規工程を止める。既に示した次工程である、要求ページ・銘柄日付集合と人工原本の一致検査を、既存strict_input_v1を変えない独立オフライン検査として実装する。実データ/APIの契約を推測せず、DB取込・既存home移行・実通信・見張りの再開へは拡大しない。完成形は売買シグナルの通知であり、自動発注を追加しない。工程と進捗はbuild-codex/COMPLETION_ROADMAP.mdへ記録する。

次の限定工程として、strict_inputで選択した実価格原本から銘柄日付を導出し、要求範囲へ結び付けるstrict_price_coverage_v1もオフラインで進める。日付は入力as_ofのtimezone基準、対象は選択価格行のみ。営業日・価格鮮度や全APIページの証明へ拡大しない。実装契約はbuild-codex/STRICT_PRICE_COVERAGE.md。

公式公開仕様の再閲覧で確認した継続キー・同一query・終端不在の規則を、人工V2応答の連鎖検査へ限定実装する（build-codex/V2_PAGE_CHAIN.md）。公開Web資料の確認は行うが、認証付きAPI・実データ取得は開始しない。9月28日予定の信用残仕様は予定として記録し、現行週次戦略やadapterを自動切替しない。

## 専用入力モードの限定採用（2026-09-12、ユーザー選択）

ユーザーは「新しい専用モードで進める」を選択した。既存研究動作を残し、部分調整・推定公表日・原本/単元/eventの不足を拒否する設計とオフライン実装を進める。最初の単位はstrict_input_v1の人工原本付き入力のメモリ内検査と安全なJSON読取CLI。DB取込、既存home移行、signal/packet/runnerへの接続、実データ取得や売買承認は含めない。ready_for_live/current_signalはfalseを維持する。以前の「市場入力の全項目が未採用」という記述より、この限定採用が優先する。外部APIの項目意味や公表時刻の根拠が未確認である点は解消しない。

## 最新継続指示（2026-09-12）

07:47以降のユーザー「codexで続けて20%まで」を受け、前回報告した残件W01a/bの期待値更新もCodexが担当する。変更は未来操作の戻り値をINVALIDに合わせる2行に限定し、後続の本物のSTOP/RESUMEの安全性assertを維持する。これは既存受入の弱体化や任意変更の許可ではない。以後の報告ではClaude原案にCodexが期待値更新したことを区別する。

ユーザーは点群処理への指示を訂正し、ai-traderのbuild-codex/Codex継続プロンプト.mdから作業を続けるよう指定した。無料リセット1回を明示依頼により使用済み。リセット後の使用率20%を今回の上限とし、以前の99%等より優先する。開始時の公式使用1%・残99%。段階ごとに使用量を報告し、20%以上で新規工程を止めて保存する。追加リセット、実通信・発注・見張り・Claude起動/公開は行わない。最新の第18回依頼W01/W02を優先する。

## 最新ユーザー訂正（2026-09-11 見張り停止）

ユーザーは前の連携再開依頼を訂正し「Claudeが制限が解除されるまでCodexとClaudeの見張りは解除して」と指示した。両見張りを停止・無効化し、ユーザーの再開指示まで起動・連携公開しない。開発はCodex単独を継続する。使用量確認30分・85%超過停止を維持する。

## 最優先の作業方針（2026-09-11 04:25 JST、ユーザー指示）

Claudeの利用制限を受け、ここからの開発・修正・テスト・レビュー・文書更新はCodex単独で継続する。Claudeの復旧・回答・レビューを作業再開の前提にしない。下記の旧役割分担に優先し、必要なops/・build-claude/の開発もCodexが担当する。共通仕様・合成データの変更禁止と実口座・実LINE・実審査LLMへの接続禁止は継続する。

これは開発担当の変更であり、売買シグナルのClaude/Codex二重承認をCodex一者で代替してよいという指示ではない。Codex自身の修正と検証はその旨を記録し、独立したClaudeレビュー済みと表記しない。ユーザーが連携再開を指示するまでClaudeへの新規引き渡し公開やClaude実行の起動は行わない。既存見張りの停止・解除・設定変更を実施した意味ではない。

現在の再開結果と残作業は build-codex/OPS_V041_RESUME.md を参照。以下の引き渡し手順・担当表は、この方針に矛盾する部分を過去の運用として扱う。

## 現在のフェーズ: **フェーズ2 後半**（2026-09-08 更新）

毎回の依頼内容は **`Codex引き渡しプロンプト.md` の「B. 今回の依頼」** に書いてある。まずそれを読むこと。
フェーズ2 前半（受入テスト先行作成・判定パケット）は完了。Claude 側の ops/（台帳・通知ゲート・ハードリミット）も
あなたの受入テスト 124 件を全通過して完了した（`ops/Claude対応結果.md`）。

役割分担（固定）:
- Codex: build-codex/（主系）、common/tests/phase2/ の受入テスト、レビュー・反証
- Claude: ops/、build-claude/（照合用副系）
- common/ の仕様・合成データは両者とも編集しない。不備は common/ISSUES.md に追記

決定済みの前提（設計書 12章）: 現物限定 / 初期リミットは 5銘柄・25%・1日2件 / 買い・売りとも寄付指値 /
損切りは証券側の逆指値 / 16:30 照合→翌 06:50 未解決で新規停止 / 台帳は `${AI_TRADER_HOME}/ledger.sqlite`。

---

## （参考）フェーズ1 の指示（完了済み）

あなた（Codex）には、Claude と**同じ仕様で同じものを別々に実装して比較する**役割をお願いします。
実装先は `build-codex/` のみ。他のフォルダは読むだけで、編集しないでください。

## まず読む文書（この順）

1. `../AI合議型自動売買システム_設計書.md`（v0.2）— 全体像。執行は「LINE通知→楽天証券Webで手動発注」。フェーズ1では通知も審査も作らない
2. `../戦略カタログ_複数ロジック検証計画.md`（v0.1）— 戦略一覧・共通基盤・採用基準。§1.6 の「対応時間」も前提
3. `common/共通仕様_フェーズ1.md`（v1.0）— **今回の実装仕様。これに厳密に従う**
4. `common/synth_data.py` — 両者共通の合成データ（変更禁止）
5. `common/tests/test_acceptance.py` — 受入テスト（変更禁止）
6. `../Claude_Codex連携仕様案.md` — あなたが以前作成した合議仕様。フェーズ2で使うため今回は参照のみ

`../_old_v0.1_*.md` は旧版なので読まなくてよい。

## 決定済み事項（設計書の未決事項に対する回答）

- 対象: 日本株、日次（スイング）。スキャルピング・日中は対象外
- 判定: Claude Code CLI ＋ Codex CLI のダブルサイン（フェーズ2）
- 執行: LINE Messaging API で通知 → 楽天証券Web で手動発注（07:00 通知、7:40 以降に発注）
- コード置き場: `999_投資関係/ai-trader/`（Dropbox）。**実行時の DB・ログ・結果本体は Dropbox 外**（`AI_TRADER_HOME`、既定 `~/.ai-trader/`）
- J-Quants: **未登録**。合成データモードで開発・テストし、実データ取り込みはトークンがあれば動く形にしておく
- 戦略 A1 のパラメータは共通仕様 §4.1 の既定値で固定（最適化禁止）
- あなたの連携仕様案にあった「confidence を安全性の根拠にしない」「仮想約定は実際の指値条件を反映する」は採用済み（共通仕様 §5 の指値規則がそれ）

## 成果物（`build-codex/` 直下）

```
build-codex/
├─ README.md            # セットアップ、CLI の使い方、設計上の判断、テスト結果
├─ requirements.txt
├─ .env.example
├─ .gitignore
├─ config/strategies.yaml
├─ aitrader/
│   ├─ __init__.py
│   ├─ __main__.py      # CLI（共通仕様 §7）
│   ├─ api.py           # init_db / load_synthetic / run_signals / run_backtest（受入テストが使う）
│   ├─ db.py
│   ├─ jquants.py
│   ├─ strategies/      # base.py, margin_bucket_long.py
│   ├─ backtest.py
│   └─ report.py
├─ results/margin_bucket_long_v1/   # 合成データ 2016-01-04〜2026-08-31 の summary.json / report.html（trades.csv, equity.csv は省略可）
└─ tests/               # 自分のユニットテスト（任意）
```

## 完了条件

1. `build-codex/` で `pytest ../common/tests -q` が **8/8 通過**
2. `python -m aitrader load-synthetic --seed 42` → `python -m aitrader backtest --strategy margin_bucket_long --from 2016-01-04 --to 2026-08-31 --out results/` が完走し、`summary.json` と `report.html` が生成される
3. README に、共通仕様で解釈が分かれうる点（例: リバランス週の重なり、未約定時の資金の扱い、営業日の扱い）を**どう解釈したか**を明記する。Claude 側との数値差の原因を人間が判定するために必要
4. 秘密情報を含めない。実口座・証券サイトに接続しない。シグナル経路で LLM を呼ばない

## 進め方の注意

- `common/` に不備を見つけたら `common/ISSUES.md` に追記する（編集はしない）。判断は人間が行う
- 「勝てるまでパラメータを探す」ことはしない。基盤の正しさと再現性を優先
- Windows でも動くように（パスは `pathlib`、コンソール出力は UTF-8）
- 完了したら README の末尾に、実行時間（10年バックテスト）と受入テストの結果を貼る

## 人間（則光）への確認が必要なとき

`build-codex/QUESTIONS.md` に質問を書いて止める。推測で仕様を変えない。

## 使用量による停止条件（2026-09-11、ユーザー指示）

Codexの使用量が85%を超えたら一度作業を停止し、このタスクでアラートを出す。公式get_usage_limitsのusedPercentを使い、使用中モデルに適用される利用枠のいずれかが85を超えた場合に適用する。85%ちょうどは超過に含めない。値不明を0扱いしない。

親および並列エージェントはまとまった処理の開始前・完了後と継続中の区切りで確認する。超過時は新規処理・追加エージェント起動を止め、親が稼働中エージェントを停止し、未完了箇所と実測使用率を通知する。リセット・クレジット購入・別モデルでの迂回や自動再開は行わず、ユーザーの再開指示を待つ。定期確認による確認間隔の間に超過する可能性があり、85%での厳密な上限制御ではない。

### 使用量確認の間隔更新（2026-09-11、ユーザー指示）

使用量確認は30分ごととし、前記の処理開始前・完了後ごとの確認に優先する。85%超過時の停止・アラート・ユーザー再開待ちは維持。既存定期確認codex-10を30分に変更済み。PowerShell見張り本体の登録や周期は変更していない。

## 並列実行の継続方針（2026-09-11、ユーザー指示）

以後、毎回の追加指示なしで独立した実装・試験・レビューへサブエージェントを活用する。現在の同時実行上限は主担当を含め4枠なので、独立して進められる作業がある限りサブエージェント3体を起動する。上限10を実行可能とは扱わない。同じファイルの書込担当は重複させず、親は統合と全体検証を担当。枠を埋めるだけの無意味なタスクは作らない。

段階完了は進捗通知し、通常の継続確認を求めず次の許可済み作業へ進む。実通信・注文の禁止、両見張り無効、30分ごとの使用量確認と85%超過停止は維持する。

### 使用量の報告方法更新（2026-09-11、最新ユーザー指示）

段階完了の進捗通知時に公式get_usage_limitsで確認したCodex使用率を併記する。30分ごとの使用量だけの通知は行わない。開発継続用の定期起動は、段階完了・障害・85%超過停止以外は静かにする。85%超過時の作業停止・アラート・再開指示待ちは維持する。

### 使用量表示の追補（2026-09-11、ユーザー指示）

段階報告にはアカウント共通のCodex利用枠について使用率と残量（100−usedPercent）の両方を表示する。複数の適用枠が返れば各枠を区別し、不明は0扱いしない。このタスクだけの消費と誤記しない。

### 定期報告の停止（2026-09-11、最新ユーザー指示）

30分ごとのCodex段階報告は不要。定期起動codex-10を削除し、作業の段階完了時にのみ結果と公式使用率・残量を報告する。作業中の通常継続は確認待ちにせず進める。85%超過時の停止・アラートは維持する。定期起動による開発再開は行わない。

### 今回の継続上限（2026-09-11、ユーザー指示）

今回の集中継続はCodexアカウント使用率30%を停止目安とする。段階ごとに公式値を確認し、30%以上なら新規段階・配下を停止して実測値と未完了を報告する。使用量を増やすためだけの作業はしない。従来85%停止は一般方針として残るが今回の30%が優先する。

### 今回の継続結果（2026-09-11 09:10 JST）

使用29%・残71%を確認し、30%直前の検証済み区切りで終了。全体1066件通過＋製品無変更の追加日次3件通過。最新状態はbuild-codex/README.md末尾、操作はMOCK_DEMO.md、残工程はPHASE2_READINESS.md。模擬シグナル表示・耐久時計・動的STOP・読取診断まで実装。実AI/LINE/注文/見張りの起動はしていない。次の作業で旧引き渡しBだけを根拠にClaude公開や見張り再開を行わない。

### 最新の分担・継続上限（2026-09-11 09:30 JST、ユーザー指示）

Astraを主担当として設計・難しい判断・最終レビュー、Solをサブエージェントとして個別実装・独立試験・文書更新に使う。現在の同時枠は主担当＋3体であり、独立した有用な仕事へ分担する。新規Solには必要な仕様とファイル所有範囲を明示し、同一ファイルの書込を重複させない。

今回の集中継続はアカウント共通Codex使用率50%を停止目安とし、以前の30%に優先する。段階通知で公式使用率・残量を表示し、50%以上なら新規工程・配下の起動を止める。開始時は29%・残71%。定期30分通知・見張り再開・実審査/LINE/注文は行わない。通常の確認待ちを挟まず、既存の模擬開発と検証を継続する。

### Astra / Sol分担の実測更新（2026-09-11 09:59 JST）

日次6シナリオ、失敗時の段階記録、SELL下流結合、完了履歴の読取専用照合まで進めた。全体1160 passed / 4 skipped / 165 warnings、245.61秒。実symlink作成4件はWindows権限不足で未実施。注入回帰と実junction拒否は別途確認。最新公式使用量33%・残67%、50%上限は変更していない。

現在の入口はbuild-codex/README.md末尾、MOCK_DEMO.md、DAILY_REHEARSAL_PLAN.md。履歴参照だけを追加採用し、run_daily_rehearsalの既存home再実行は拒否する。INTENTの自動書込復旧、永続STOP予約前統合、売却戦略の新設は未実装。旧引き渡しBを理由に見張り再開・Claude起動・公開をしない。進捗記録やVERIFIED_HISTORYだけを根拠に修復・予約解除しない。

### 停止診断の実測更新（2026-09-11 10:22 JST）

opsの公開inspect_stop_statusと主系の読取CLIを追加。全体1213 passed / 5 skipped / 165 warnings、236.40秒。最新公式使用35%・残65%、50%上限維持。読取診断だけの採用で、制御DB作成・STOP/RESUME書込・runner予約前接続はしていない。次はSTOP_INTEGRATION_PLAN.mdの初期化・直列化契約を確認する。CLEARを売買承認に転用せず、UNKNOWNを停止なしに補完しない。操作はMOCK_DEMO.md末尾、実測はREADME.md末尾を参照する。

### 管理STOPの予約前接続（2026-09-11 11:00 JST）

新規home限定managed-v1を採用。停止ストア初期化、共通runner-lock下の明示STOP/RESUME、runnerの審査時・BUY予約直前の停止確認を実装。全体1228 passed / 5 skipped / 165 warnings、226.52秒。その後の追加確定とlegacy型厳密化は対象35件18.84秒で検証。全体の最終版再実行とは表記しない。公式使用37%・残63%、50%停止目安維持。

次はbuild-codex/Codex継続プロンプト.mdから日次CLIの明示managed選択と配信前確認共通化を進める。旧homeの自動移行、INTENT復旧、実通信、Claude公開、両見張り起動は行わない。停止管理clockは明示制御の観測を保持するもので、全外部書込の時系列保証ではない。詳細はREADME末尾とSTOP_INTEGRATION_PLAN.md。

### 日次managed・配信接続の確定（2026-09-11 11:32 JST）

新規homeの --managed-stop、通知計画の審査時policy照合、配信前停止検査、enqueue/delivery/reconcileの共通managed clockを接続。全体1267 passed / 5 skipped / 195 warnings、307.88秒。managedの6日次シナリオも確認、実送信なし。公式使用39%・残61%、50%停止目安維持。実審査/LINE/注文/両見張り/Claude公開は起動しない。

次はbuild-codex/Codex継続プロンプト.mdの模擬ステータス表示と残契約整理。現在STOPと保存履歴を区別し、履歴参照から自動復旧・予約解除をしない。最新実測はREADME末尾、PowerShellフォルダ移動・操作はMOCK_DEMO.md。

### 集中継続の再確認（2026-09-11）

ユーザーの「残り50%は継続して進めて」に従い、公式アカウント使用率50%・残量50%を停止目安として継続する。開始時39%使用・61%残。段階ごとに通知し、通常の継続確認を求めない。使用量を増やすだけの作業はせず、模擬運用準備の有用な段階を進める。

### 模擬運用準備の確定（2026-09-11 12:19 JST）

状態表示、明示STOP CLI、診断/管理ファイル/JSON保存/packet mappingを補強。全体1377 passed / 9 skipped / 495 warnings、339.49秒。その後JQuantsClientの応答・例外安全性を補強し、関連35件（新規31＋既存4）通過。後者込み全体は未再実行。アカウント使用48%・残52%、50%手前の検証済み区切りで終了。次はbuild-codex/Codex継続プロンプト.mdから開始する。実通信・発注・見張り・Claude公開の停止方針は維持。

### 最新の集中継続上限（2026-09-11、ユーザー指示）

ユーザーの「次は70%まで」に従い、今回の停止目安をアカウント共通Codex使用率70%（残30%）へ変更する。以前の50%に優先する。開始時48%・残52%。Astra主担当＋Sol最大3体を独立作業へ分担し、段階ごとに公式使用率・残量を報告、通常の継続確認なしで進める。70%以上では新規工程を開始せず検証結果と未完了を保存する。無料リセットは使用しない。実通信・発注・見張り・Claude公開の禁止は継続する。

### 今回の検証済み区切り（2026-09-11 13:53 JST）

全体1885 passed / 9 skipped / 517 warnings、260.70秒。使用65%・残35%、70%上限内の区切りで終了。市場入力境界、成果物安全公開と読取診断、独立来歴fixture、V2形状検査、来歴意味整合と公開後cleanup保持を確定した。実データ入力契約・V2実接続・INTENT復旧は未採用。続きはbuild-codex/Codex継続プロンプト.mdを読む。見張り・Claude公開・実AI/LINE/注文の起動禁止、共通仕様・合成データ無変更を維持する。

### V2ファイル検査追加（2026-09-11 13:59 JST）

独立V2検査CLI追加、新規19件と関連180件通過（0.83秒）。前回全体1885件は保持し、追加後の全体再実行とは表記しない。使用65%・残35%、70%上限維持。次はbuild-codex/Codex継続プロンプト.md。PowerShell手順はV2_FIXTURE_INSPECTION.md。実通信・DB書込・候補経路への接続はない。

### 最新の集中継続上限（2026-09-11、ユーザー指示）

ユーザーの「次 上限80%で行きましょう」に従い、停止目安をCodexアカウント共通使用率80%へ更新する。以前の70%に優先する。開始時69%・残31%。Astra主担当＋Sol最大3体で独立作業を分担し、段階報告に使用率と残量を表示する。80%以上で新規工程を始めず保存する。無料リセット・実通信・発注・見張り・Claude公開は起動しない。

### 主系境界補強の最終実測（2026-09-11 14:50 JST）

全体1962 passed / 9 skipped / 517 warnings、272.91秒。raw home親・主要leaf・SQLite sidecar、日次receipt直接API、未完run候補所有/判定/outbox原本を補強。使用72%・残28%、80%上限維持。次回はbuild-codex/Codex継続プロンプト.mdを入口とする。完了runの履歴再読は維持し、現在の再承認やINTENT復旧へ変えない。実通信・見張り・Claude公開禁止は維持。

### 通知原本・診断境界の確定（2026-09-11 16:10 JST）

通知直前candidate原本照合を追加し全体1979 passed / 9 skipped（273.82秒）。その後の深いJSON診断2件と例外抑止は関連47 passed（15.85秒）、追加後全体は未再実行。使用75%・残25%、80%上限維持。続きはbuild-codex/Codex継続プロンプト.md。

### 最新の集中継続上限（2026-09-11、ユーザー指示）

「次は90%までどんどん進めて」に従い、今回の停止目安をCodex共通使用率90%へ更新する。今回に限り従来80%上限および85%超過停止に優先する。段階報告で公式使用率と残量を確認し、90%以上で新規工程を始めず状態を保存する。通常の継続確認なしで有用な作業を進め、使用量を増やすだけの作業はしない。無料リセット・実通信・発注・見張り・Claude公開の禁止は維持する。

### 最新の集中継続上限（2026-09-11、ユーザー指示）

「次は95%までどんどん進めて」に従い、今回の停止目安をアカウント共通Codex使用率95%へ更新する。以前の90%・80%および一般85%停止より優先する。段階ごとに公式使用率・残量を表示し、95%以上で新規工程を止めて保存する。開始時85%・残15%。無料リセット、実通信、発注、見張り、Claude公開は開始しない。

### 最新の集中継続上限（2026-09-11、ユーザー指示）

「次は98%までどんどん進めて」に従い、今回の上限はアカウント共通Codex使用率98%。以前の95%および一般85%停止より優先する。開始時92%・残8%。段階ごとに公式使用率と残量を確認し、98%以上で新規工程を開始せず保存する。リセット、実通信、発注、両見張り、Claude公開は開始しない。

### 最新の集中継続上限（2026-09-11、ユーザー指示）

「99%まで進めて」に従い、今回の上限をCodexアカウント使用率99%へ更新する。以前の98%等より優先。開始時95%・残5%。99%以上で新規工程を止めて保存する。段階報告で使用率と残量を表示し、リセット・実通信・注文・見張り・Claude公開は開始しない。
